name: to implement cicd

on:
  push:
    branches: [main,amardeep,vishal]
  pull_request:
    branches: [main,amardeep,vishal]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install Packages
        run: |
          pip install coverage

      - name: Display Branch Name
        run: |
           echo ${GITHUB_HEAD_REF}

      - name: Unit Test
        run: |
          ls -al
     
